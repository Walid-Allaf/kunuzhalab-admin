import { Box, Typography } from "@mui/material";

const Home = () => {
  return (
    <Box sx={{ display: "grid", minHeight: "90vh", placeItems: "center" }}>
      <Typography variant="h3">kunuz halab admin dashboard</Typography>
    </Box>
  );
};

export default Home;
